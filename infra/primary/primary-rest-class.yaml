apiVersion: platform.confluent.io/v1beta1
kind: KafkaRestClass
metadata:
  name: primary-cluster-restclass
  namespace: confluent
spec:
  kafkaClusterRef:
    name: kafka
    namespace: confluent
  kafkaRest:
    authentication:
      type: mtls
    tls:
      secretRef: rest-class-tls
